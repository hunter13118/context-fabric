# Context Fabric

Enterprise AI context layer — ingests cross-app signals, builds a permission-aware context graph, and serves grounded summaries, meeting briefs, and MCP tools.

| Path | What |
|---|---|
| `Context-Fabric-Architecture-Spec.md` | Full architecture spec |
| `context-fabric/` | Runnable TypeScript prototype (REST API + MCP + tests) |
| `web/` | Vite/React showcase and demo UI |

See `context-fabric/README.md` for quick start (`npm install && npm run demo`).
